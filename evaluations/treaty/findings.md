# treaty — Findings

| Failure mode | Title | Severity | Score | Date | Notes |
|---|---|---|---|---|---|
| §10 | Interactivity & TTY Requirements | Critical | 3/3 | 2026-09-24 | No prompt/pager/editor code paths exist; init, exec, audit, conformance and a destructive example all exit well under 5s with stdin=/dev/null and PAGER/EDITOR/VISUAL=`sleep 30`; destructive commands refuse with CONFIRMATION_REQUIRED (exit 2) instead of prompting, even under a PTY; non-TTY stdout auto-switches to JSON ✓ |
| §34 | Shell Injection via Agent-Constructed Commands | Critical | 3/3 | 2026-09-25 | Exec arrays only (no shell=True); `init acme%2Fwidgets` rejected by name regex (type-level), but `init --directory ../../etc/test` and `conformance --out ../../etc/test.json` accepted silently; the latter wrote outside the repo. Re-scored 2026-09-25: `Path` fields reject `..`, `%XX` and null bytes framework-wide before any handler runs, on every input route, with `rejected_pattern` and a resolved suggestion |
| §37 | REPL / Interactive Mode Accidental Triggering | Critical | N/A | 2026-09-24 | Not applicable: neither treaty nor the framework has a REPL or shell mode; every command is one-shot. No `requires_interactive` concept in the schema |
| §42 | Debug / Trace Mode Secret Leakage | Critical | 3/3 | 2026-09-25 | Originally 0/3: no `sensitive` declaration and parse errors echoed raw values. Now `secret=` (declared or inferred from the name) redacts the value as `[REDACTED]` in every error on every input route, and secrets never enter argv at all; there is still no --debug/--trace mode to leak through |
| §43 | Tool Output Result Size Unboundedness | Critical | 0/3 | 2026-09-24 | No `meta.truncated`, `meta.total_bytes`, `--max-output` or per-command output limit; `treaty manifest` returns 7.8KB in full and `exec` emitted 2.4MB for a 228KB plan |
| §45 | Headless Authentication / OAuth Browser Flow Blocking | Critical | N/A | 2026-09-24 | Not applicable: treaty has no authenticated commands and the framework has no auth flow; framework only reserves AUTH_REQUIRED (exit 8), with no `auth_methods` or `requires_auth` schema field |
| §50 | Stdin Consumption Deadlock | Critical | 2/3 | 2026-09-24 | `exec` (only stdin reader) exits in 0.1s with a structured EMPTY_STREAM error + `fix_required` on closed stdin and STDIN_IS_TTY on a terminal; code name differs from `STDIN_REQUIRED` and schema has no `stdin_fallback`/`non_tty_behavior` |
| §53 | Credential Expiry Mid-Session | Critical | N/A | 2026-09-24 | Not applicable: no credentials in treaty; framework has AUTH_REQUIRED (8) and PERMISSION_DENIED (7) but no CREDENTIALS_EXPIRED or `reauth_command` |
| §60 | OS Output Buffer Deadlock | Critical | 1/3 | 2026-09-24 | Envelopes are flushed explicitly, so `exec` streams one line per completed request (1s apart for 1s requests); no heartbeat, so a single 5s command emits nothing until it finishes |
| §61 | Bidirectional Pipe Payload Deadlock | Critical | 0/3 | 2026-09-24 | `exec` streams output while still reading input and has no stdin size limit or `--input-file`; a caller that writes a 228KB plan before reading blocked (write not finished after 5s) until stdout was drained |
| §62 | $EDITOR and $VISUAL Trap | Critical | N/A | 2026-09-24 | Not applicable: no command or framework path launches an editor; `EDITOR="sleep 30"` had no effect during §10 |
| §64 | Headless Display and GUI Launch Blocking | Critical | N/A | 2026-09-24 | Not applicable: no command or framework path opens a browser or GUI |
| §71 | Non-Interactive Installation Absence | Critical | 1/3 | 2026-09-24 | `uv pip install .` works non-interactively and is idempotent (exit 0 twice, `treaty --version` verifies), but README has no install section and there is no AGENTS.md; the gitignored `dist/` wheel is stale and installs without a `treaty` executable |
| §11 | Timeouts & Hanging Processes | Critical | 2/3 | 2026-09-24 | Framework `--timeout` with TIMEOUT exit 10, structured error and `meta.duration_ms`/`timeout_ms` (fetch 5s --timeout 2 exited in 2.07s); no `completed_steps`, `resume_token` or heartbeat. Treaty's own commands have no network I/O, so checked via examples/slowctl.py |
| §12 | Idempotency & Safe Retries | Critical | 0/3 | 2026-09-24 | No `--idempotency-key` (rejected as unknown flag) and no `effect` field; retrying `init` into the same directory returns CONFLICT (exit 6) instead of duplicating, and `conformance --out` overwrites the same file, so retries are safe in practice |
| §13 | Partial Failure & Atomicity | Critical | 1/3 | 2026-09-24 | `exec` emits a structured envelope per line with `_line`/`_cmd`, stops at the failing line 3 and exits 1; no `partial: true`, resume token or rollback, and exit 1 (per REQ-O-050) does not distinguish partial failure from general error |
| §23 | Side Effects & Destructive Operations | Critical | 2/3 | 2026-09-24 | Checked via examples/deployctl.py: framework requires a dry_run field on destructive commands, declares `danger_level` in the manifest and refuses without `--confirm-destructive`; `--dry-run` exits 0 with `applied: false` (a per-command field, not a framework `effect`); no audit output |
| §24 | Authentication & Secret Handling | Critical | 3/3 | 2026-09-25 | Originally 0/3: the only secret path was a regular flag. Now a secret field is only reachable via `--x-from-env`, `--x-from-file` or the `<APP>_<X>` default listed in `secret_env_vars`; a direct value exits 2 without echoing it, and a missing var or file exits 2 in phase 1 |
| §25 | Prompt Injection via Output | Critical | 0/3 | 2026-09-24 | Handler data is returned untagged under `data` (envelope separates it from ok/meta/error, but no `trusted`/`_content_type`); treaty's own commands limit exposure: `audit` of a module with an injection-text description did not echo it |
| §74 | Credential Scope Declaration Absence | Critical | 2/3 | 2026-09-24 | Every manifest entry carries `required_scopes` (all `[]` for treaty, declared via `required_scopes=` on commands); no `check-permissions` pre-flight or over-privilege warnings |
| §75 | Safe-Default Execution Mode Absent | Critical | 1/3 | 2026-09-24 | Destructive commands default to a refusal with a preview (exit 2, `applied: false`), not live execution, but there is no `safe_default`, `--live`, `would_*` effect or `meta.dry_run`; checked via examples/deployctl.py |
| §1 | Exit Codes & Status Signaling | Critical | 3/3 | 2026-09-24 | Distinct documented codes: missing arg 2 (ARG_ERROR), missing module 5 (NOT_FOUND), timeout 10; `meta.exit_code` always matches; each command declares its codes in the manifest |
| §2 | Output Format & Parseability | Critical | 3/3 | 2026-09-24 | Full envelope (`ok`, `data`, `error`, `warnings`, `meta` with `request_id`, `duration_ms`) on every command; JSON auto-selected when stdout is not a TTY or `CI` is set; the flag is `--format json` (`--output json` is rejected) |
